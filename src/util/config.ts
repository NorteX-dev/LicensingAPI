// Port of the server to listen on.
export const PORT = 2000;

// Requests authorized with below api keys are considered sudo.
// This means any route is unprotected with this key.
// Beware when sharing this (these) key.
export const API_KEY = ["secretcat5"];
